
services:

  portfolio:
    image: greatmike1/portfolio-app:latest
    container_name: portfolio-app
    ports:
      - "80:80"
    restart: always

  java-app:
    image: greatmike1/java-app:latest
    container_name: java-app
    ports:
      - "8080:8080"
    restart: always
